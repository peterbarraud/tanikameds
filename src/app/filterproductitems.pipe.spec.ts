import { FilterProductItemsPipe } from './filterproductitems.pipe';

describe('FilterProductItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
